#include "FRect.h"

namespace Global::Definitions
{
/// <summary> Setups all the Fields from the FRect Instance. </summary>

FRect::FRect()
{
this->mX = 0.0f;
this->mY = 0.0f;

this->mWidth = 0.0f;
this->mHeight = 0.0f;
}

}